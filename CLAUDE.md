# Youtube Scout — 구현 지시서 (CLAUDE.md)

> 이 문서는 자기완결이다. 외부 파일 참조 없이 이 문서만으로 전체를 구현한다.
> 모든 설계 결정은 2026-07-30 실측과 6개 결정(D1~D6)으로 확정됐다. **재검토하지 마라.**
>
> **동기화 기준: v0.2.10 / 2026-09-22 / 커밋 `2a3f544`.** 1~18장은 그 시점까지의 명세이고,
> 19~22장이 그 뒤에 들어간 것을 코드에서 추출해 채운 분량이다.
> **정본은 코드다.** 이 문서가 코드와 어긋나면 코드가 맞고 이 문서가 틀린 것이다 —
> 그때 할 일은 코드를 문서에 맞추는 것이 아니라 문서를 고치는 것이다.
> 역할 분리: 이 문서 = 지켜야 할 **계약**. `README.md` = **사용법**.
> `docs/design-story.md` = 실측이 설계가 된 **경위**. `docs/harness-design.md` = 하네스 **근거**.
> 경위가 궁금하면 복제하지 말고 design-story의 절 제목을 가리킨다.

---

## 0. 정체성 — 흔들리면 안 됨

**정찰병(Scout)이다. 요약기가 아니다.**
유튜브 링크를 넣으면 자막이 놓친 **화면 정보까지 회수**해 구조화된 보고서를 낸다.
본 것을 등급으로 구분해 보고할 뿐 판단하지 않는다.
차별점: ①화면에만 있던 정보 ②발표자가 입력한 원문 ③다루지 않은 것 ④이해관계 신호.

### 확정된 결정 (변경 금지)
- 전면 무료 오픈소스, MIT 단일 라이선스, 저장소 `youtube-scout`
- 배포 형태: **CLI 단독** (Node, `npx github:p7000stars-art/youtube-scout` 실행 지원)
- 하네스 전면 공개 + 1급 시민 (별도 문서로 설계 근거 서술)
- 메타 취득: watch 페이지 파싱 (Data API 사용 안 함)
- 스택: **JS(ESM) + JSDoc + `// @ts-check`**. TypeScript 금지(무빌드 원칙 — 소스=실행물이 검증 가능성의 근거)
- 런타임 의존성 **0** (Node 20+ 내장 fetch·`node:util` parseArgs·`node:test`만 사용)

---

## 1. 절대 규칙

1. **모든 코드에 한글 주석 필수.** "무엇을"이 아니라 "왜"를 쓴다. 실측 근거가 있는 로직은 주석에 근거를 명시한다 (예: `// 실측 2026-07-30: RPD는 대기해도 안 풀림 → 즉시 모델 교체`)
2. **해상도는 HIGH 상수 고정.** `MEDIA_RESOLUTION_LOW`라는 문자열이 저장소 어디에도 존재하면 안 된다. 옵션으로도 노출 금지. (LOW는 작은 글자를 날조하고 날조 사실조차 보고하지 않음 — 실측)
3. **API 키는 `GEMINI_API_KEY` 환경변수로만.** CLI 인자로 받지 않는다(셸 히스토리 유출 차단). 키를 로그·산출물·에러 메시지에 절대 출력하지 않는다.
4. **하네스를 코드에 하드코딩하지 않는다.** `prompt/meeting-v1.md` 파일에서 읽어 `systemInstruction`으로 주입한다.
5. **요약 기능·자동 판단·추천 기능을 추가하지 않는다.** 회원 전용 영상 우회 기능을 추가하지 않는다.
6. `src/` 모듈은 `process.argv`·`console`·`process.exit`를 직접 사용하지 않는다. 입출력은 전부 `bin/`이 담당한다. (코어/껍데기 분리 — 웹 확장 여지)
7. 저장소에 개인 경로·개인 링크·키가 단 한 줄도 들어가면 안 된다. 커밋 전 `scripts/check-leak.js`를 실행한다.
8. 산출물(.md)·소스는 UTF-8 BOM 없음, LF.
9. **`package.json`의 `version`은 CI가 자동으로 올린다. 수동으로 수정하지 마라** — main 머지마다 `version-bump.yml`이 patch를 올리므로, 사람이 같이 건드리면 충돌이 난다. (사람이 올리는 방식은 이미 실패했다 — 기능 머지 8회 동안 0.2.0에 고정돼 있었고, 그 사이 산출물 여덟 판이 전부 같은 `scout_version`을 달았다)

---

## 2. 저장소 구조

**(개정 2026-09-22)** 아래는 v0.2.10 `git ls-files`의 전량이다. 원문은 1차 구현 시점의
골격이었고, 그 뒤 파일 22개가 늘었다(8개 모듈 + 테스트 10개 + 워크플로 2개 + 픽스처·문서 2개).
낡은 구조도는 "이게 전부"라는 오정보원이 되므로 실물로 교체한다.
원문 기록: `bin/youtube-scout.js` 하나, `src/` 6종(meta·plan·extract·quota·merge·output),
`test/` 3종(plan·quota·merge), `docs/harness-design.md`, `fixtures/` 2종, `scripts/check-leak.js`.

```
youtube-scout/
├─ package.json                 # "type":"module", bin, engines.node ">=20". version은 CI가 올린다(1장 9)
├─ LICENSE                      # MIT
├─ README.md                    # 사용법 (계약은 이 문서가 진다)
├─ CLAUDE.md                    # 이 문서
├─ .gitignore                   # Node 기본 + out/ + youtube-scout-run/ + model-status.txt + .check-leak.local + .env
├─ bin/                         # 껍데기 층 — 콘솔·파일시스템·종료를 아는 곳
│  ├─ youtube-scout.js          # CLI 진입점. 인자 파싱·부팅·배선·요약 (3-7, 19-0)
│  ├─ chunk-runner.js           # 구간 하나의 재시도·모델 교체 루프 (19-4)
│  ├─ exit.js                   # finishProcess — process.exit 금지 절차 (19-5)
│  ├─ log-queue.js              # _batch.log 쓰기 직렬화 (19-6)
│  ├─ ui.js                     # 스피너·막대·색·표 칸 계산 (19-7)
│  └─ update-check.js           # npx 캐시 갱신. 독립 실행 스크립트이자 모듈 (19-8)
├─ src/                         # 코어 층 — process.argv·console·process.exit를 쓰지 않는다(1장 6)
│  ├─ meta.js                   # watch 페이지 파싱 + playabilityStatus 사전 차단 (3-1)
│  ├─ plan.js                   # 청크 계획 + 요청 수 예산(budgetEstimate) (3-2)
│  ├─ extract.js                # Gemini 호출 (HIGH 고정, 하네스 주입) (3-3)
│  ├─ quota.js                  # 429 본문 해석 + ModelPool 4집합 (3-4)
│  ├─ merge.js                  # 계획 기반 병합 (glob 금지) (3-5)
│  ├─ output.js                 # frontmatter·각인·폴더/파일명·배너 (3-6)
│  ├─ models.js                 # 모델 목록 조회·정렬·풀 결정 (14장)
│  ├─ model-status.js           # 모델 상태 파일 파싱·상태 전이 (19-3)
│  ├─ init.js                   # 실행 환경 생성·갱신 (15장, 19-2)
│  └─ http.js                   # discardBody — 버리는 응답 본문 닫기 (19-1)
├─ prompt/
│  └─ meeting-v1.md             # 하네스 (10장 그대로. 한 글자도 수정 금지 — 내용 해시가 각인된다)
├─ docs/
│  ├─ harness-design.md         # 하네스 각 장치의 근거 (11장)
│  └─ design-story.md           # 실측이 설계가 된 경위 11절. 경위는 여기만 쓴다
├─ fixtures/
│  ├─ member-only.txt           # 회원 전용 영상 — 사전 차단 회귀 픽스처 (12장)
│  ├─ quota-429-rpd.json        # 429 RPD 응답 본문 (12장)
│  ├─ models-list.json          # /v1beta/models 응답 — 필터 검증용 (17장)
│  └─ models-list-mixed.json    # 같은 응답의 실측 순서 재현 — 정렬·자동 모드 검증용
├─ test/                        # 전부 node:test. 네트워크를 치는 테스트는 만들지 않는다
│  ├─ plan.test.js              # 경계 계산 + budgetEstimate
│  ├─ quota.test.js             # 429 파싱 + ModelPool
│  ├─ merge.test.js             # 계획 기반 병합 + ⛔ + 잔여
│  ├─ output.test.js            # 파일명·폴더명·frontmatter·각인
│  ├─ models.test.js            # 필터·대조·정렬·resolvePool·별칭 판정
│  ├─ model-status.test.js      # 상태 파일 왕복 + 3단계 전이 + rpd 소거
│  ├─ chunk-runner.test.js      # 재시도·교체 순서 (실 API로는 재현 불가한 조합)
│  ├─ init.test.js              # 생성물 ASCII·CRLF·미덮어쓰기·MODELS 줄 갱신
│  ├─ ui.test.js                # 폭 계산·막대·색·배너 안전망
│  ├─ log-queue.test.js         # 쓰기 순서 보장 + 실패해도 큐가 죽지 않음
│  ├─ exit.test.js              # 종료 코드 설정 + unref 안전망
│  ├─ cli-exit.test.js          # 진입점이 process.exit를 직접 부르지 않는지
│  ├─ update-check.test.js      # 캐시 항목 판별·시각 비교·삭제 재확인
│  └─ bump-version.test.js      # version 줄 하나만 바뀌는지
├─ scripts/
│  ├─ check-leak.js             # 유출 검사 (7장)
│  └─ bump-version.js           # package.json patch +1. CI가 부른다 (20장)
└─ .github/workflows/
   ├─ test.yml                  # OS 3 × Node 2 매트릭스 (20장)
   └─ version-bump.yml          # main 머지마다 patch 증가 (20장)
```

`package.json` 핵심:
```json
{
  "name": "youtube-scout",
  "type": "module",
  "bin": { "youtube-scout": "./bin/youtube-scout.js" },
  "engines": { "node": ">=20" },
  "scripts": { "test": "node --test", "check-leak": "node scripts/check-leak.js" }
}
```

---

## 3. 모듈 명세

각 모듈이 지키는 실측 함정을 주석으로 새길 것. 기본값은 전부 실측 검증치다.

### 3-1. `src/meta.js`
- `https://www.youtube.com/watch?v=<ID>` HTML을 fetch (타임아웃 60초)
- 파싱 대상 (정규식):
  - `"lengthSeconds":"(\d+)"` → 초 단위 길이 (없거나 0이면 실패 처리)
  - `"ownerChannelName":"(.*?)"` → 채널명
  - `<meta property="og:title" content="(.*?)">` → 제목 (HTML 엔티티 디코딩 필요)
  - `"playabilityStatus":\{"status":"([A-Z_]+)"` → `OK`가 아니면 재생 불가
  - reason: `"playabilityStatus":\{"status":"[A-Z_]+","reason":"(.*?)"` → 사유 문구
- 반환: `{ id, url, sec, title, channel, playable, blockReason, status }` **(개정 2026-09-22.**
  원문은 `status` 없는 7필드였다. `status`는 playabilityStatus 원문이고, 사유 문구를 만들지
  못했을 때 `재생 불가 (playabilityStatus=…)`로 되살리는 근거다)
- 길이 0은 `playable=false`로 떨어뜨린다 — 진행 중 라이브·프리미어는 구간(startOffset/endOffset)을
  정할 수 없다. 사유 문구도 그렇게 말한다 (status는 `OK`인데 처리 불가인 유일한 갈래)
- 링크 인식은 `/live/`도 포함한다 (라이브 아카이브)
- **왜 사전 차단인가 (주석에 쓸 것)**: 회원 전용/비공개 영상도 제목·길이가 정상 노출된다. 확인 없이 진행하면 실패할 걸 알면서 청크 수만큼 API를 두드리고, 403 이후 429 스로틀링까지 유발한다 (실측: 사전 차단으로 API 5회→0회, 16초→5초)
- 링크→ID 추출도 이 모듈에: `youtu.be/`, `watch?v=`, `/shorts/`, `/embed/`, 순수 11자 ID 전부 허용. 패턴 `([A-Za-z0-9_-]{11})`
- **이 모듈은 단일 격리 지점이다**: 유튜브 페이지 구조 변경 시 여기만 고치면 되도록, 다른 모듈이 HTML을 만지지 않게 한다 (Data API 폴백 재검토 트리거: 이 파서의 실파손)

### 3-2. `src/plan.js`
- 입력: 영상 길이(초), 청크 길이(기본 **480**), 겹침(기본 **5**)
- 청크 경계: 첫 청크는 0부터, 이후 청크는 `이전 끝 - 겹침`부터. 마지막은 영상 끝에서 자름
- 전 영상 합산 예상 요청 수 계산 → `{ ranges, totalCalls, exceedsQuota }` 반환 (일일 한도 기본 **20**)
- **왜 예산 계산인가**: 청크 분할이 요청 수를 곱한다. 실측 사건 — 6편 투입 = 약 30요청 → 한도 20 즉시 초과, 첫 청크부터 429. 공식 문서는 1,500회라고 하지만 실제 할당은 20회였다
- 판단·출력은 하지 않는다. 초과 여부만 구조체로 반환하고 진행 확인은 bin의 몫
- **한도 추정은 `budgetEstimate`가 한다 (추가 2026-09-22).** 입력은 개수가 아니라 **모델명**이다
  (`{ pool, demoted, blocked, aliases, perModel }`) → `{ dailyLimit, usable, basis, allFailed, note }`.
  세는 규칙: 풀에서 강등(demoted)·차단(blocked)·별칭(alias)을 뺀 나머지 × 모델당 20회.
  이름으로 받는 이유는 **강등된 별칭을 두 번 빼지 않기 위해서**다(집합 연산이라 중복 차감이 불가능하다).
  셀 것이 0이면 전체 풀로 계산하고 그 사실을 `note`에 적는다 — 상시 경고는 아무도 읽지 않는다.
  근거: 실측 2026-07-31에 15종 풀의 실질 가용이 4종이었는데 화면에는 300회로 떴다.
  "무엇이 별칭인가"의 판정은 `models.js`(`isAliasModel`)가 하고, 여기서는 집합 연산만 한다
- `quotaAlternatives`는 한도 초과 시 낼 대안 3개 문구만 만든다 (출력은 bin)

### 3-3. `src/extract.js`
- 엔드포인트: `POST https://generativelanguage.googleapis.com/v1beta/models/<모델>:generateContent`
- 헤더: `x-goog-api-key`, `Content-Type: application/json`
- 요청 본문:
```js
{
  systemInstruction: { parts: [{ text: 하네스전문 }] },
  contents: [{ parts: [
    { fileData: { fileUri: 'https://www.youtube.com/watch?v=' + id },
      videoMetadata: { startOffset: start + 's', endOffset: end + 's' } },
    { text: 청크안내문 }
  ]}],
  generationConfig: {
    temperature: 0.1,
    maxOutputTokens: 32768,
    mediaResolution: 'MEDIA_RESOLUTION_HIGH'   // 상수. 변수·옵션화 금지
  }
}
```
- 청크 안내문 (그대로 사용): `이 구간(${start}초~${end}초)을 시스템 지침의 관찰 규율과 출력 형식에 따라 분석하라. 이것은 더 긴 영상의 일부다. 앞뒤 맥락이 잘렸을 수 있으니 구간 밖을 추측하지 마라.`
- 타임아웃 **300초** (AbortController). 성공 시 `candidates[0].content.parts[].text` 합쳐 반환 + `usageMetadata.totalTokenCount`
- 실패 시 HTTP 상태·응답 본문을 그대로 quota.js 해석기에 넘긴다. **예외 메시지만 보면 원인을 알 수 없다** (실측: 429의 정체가 quotaId에만 있었음)

### 3-4. `src/quota.js`
- 429 응답 본문에서 파싱: `"quotaId":\s*"([^"]+)"`, `"quotaValue":\s*"(\d+)"`, `"retryDelay":\s*"(\d+)s"`
- **429는 두 종류이고 대응이 정반대다 (핵심 주석)**:
  - quotaId에 `PerDay` 포함 → **RPD. 대기해도 안 풀린다. 재시도 금지, 즉시 모델 교체 신호** (실측: 4초 지연을 넣은 실행에서도 RPD가 터짐. 기존 코드가 회복 불가 상황에 60초×3을 낭비했음)
  - 그 외(TPM/RPM) → 서버 권고 `retryDelay` + 3초 대기 (상한 **90초**)
- **(개정 2026-09-22) TPM 재시도 예산은 3회가 아니라 한 모델당 2시도(대기 1번)다.** 판정은
  `bin/chunk-runner.js`가 하고 이 모듈은 대기 시간만 계산한다. 원문은 `재시도 최대 3회`였고,
  `MAX_RETRIES = 3` 상수도 아직 여기 남아 있지만 **재시도 경로는 그 값을 쓰지 않는다**
  (근거는 아래 표의 TPM 행). `quotaMessage`가 재시도 횟수를 말하지 않는 이유도 같다 —
  경로마다 예산이 다르므로 한쪽 값을 적으면 다른 쪽에서 거짓말이 된다
- **응답별 대응은 네 갈래다 (추가 2026-09-22).** 해법이 전부 다르므로 안내도 갈라서 낸다.
  뭉쳐서 알리면 사용자가 엉뚱한 조치를 한다. 판정·교체는 `chunk-runner`, 제외 집합은 `ModelPool`,
  다음 실행까지 끌고 갈 관찰인지는 `model-status.js`가 정한다.

  | 사건 | 대기 | 이번 실행 | 상태 파일 | 사용자 해법 |
  |---|---|---|---|---|
  | 429 RPD (`quotaId`에 `PerDay`) | 안 한다 | `exhausted`로 제외, 즉시 교체 | `rpd` 기록 (자정 이후 소거, blocked로 승격 안 함) | 내일 재실행 (완료 구간은 건너뛴다) |
  | 429 TPM/RPM | 권고+3초, 상한 90초. **한 번만** | 2번째 TPM이면 `tpmBlocked`로 제외, 교체 | `tpm-<실패 구간 초>` 기록 | `--chunk`를 줄이면 그 모델도 쓴다 |
  | 404 (퇴역·좀비 모델) | 안 한다 | `dead`로 제외, 즉시 교체 | `404` 기록 | 목록에서 제거 — 내일도 안 된다 |
  | 5xx (서버 과부하) | 10초. **한 번만** | `overloaded`로 제외, 교체 | **남기지 않는다** | 잠시 후 다시 실행 |

  - TPM에서 한 번만 기다리는 근거: 개인 키 단독 사용이라 권고 대기 뒤에는 분당 창이 비어 있다.
    그런데도 막히면 **요청 하나가 분당 한도보다 큰 것**(구조적 TPM)이고 시간이 해결해 주지 않는다.
    실측 2026-07-30 — 권고 대기를 지킨 4시도·약 3분이 판정에 아무 정보도 더하지 못했다.
  - 5xx도 같은 구조인 근거: 예전 전제("혼잡은 시간이 해결한다")가 실측 2026-07-31에 반증됐다.
    10·20·30초를 모두 지키고도 같은 모델이 계속 503이었고, 가용 10종을 안 써 보고 6분 39초를
    태운 뒤 산출물 0개였다. **교체가 대기보다 압도적으로 싸다.**
  - 503만 상태 파일에 안 남기는 근거: 503은 모델의 속성이 아니라 그 순간 서버 사정이다.
    남기면 멀쩡한 모델이 내일 강등된 채 시작하고, 회복이 분 단위라 날짜로 거를 수도 없다.
- 모델 풀 관리: 쿼터는 `PerProjectPerModel` — 모델별 분리. 영상 단위로 순환 배정(한 영상의 청크는 동일 모델 — 문서 내부 일관성). RPD 소진 모델은 이번 실행 동안 풀에서 제외. 전 모델 소진 시 잔여 영상 이월(재개 지원이 받아줌)
- **(개정 2026-09-22) 제외 집합은 네 개다**: `exhausted`·`dead`·`tpmBlocked`·`overloaded`.
  원문은 RPD 하나였다. 넷을 나눈 이유는 오로지 **해법이 다르기 때문**이고, 요약표도 네 줄로 나눠 낸다
- **교체용 모델은 순환이 아니라 우선순위 순서로 고른다** (`nextAvailable`). 영상 배정(`assign`)은
  커서 순환이지만 교체에 커서를 쓰면 검증된 앞쪽 모델이 남아 있는데 꼬리의 미검증 모델로 넘어간다 —
  "검증 풀이 소진됐을 때만 꼬리를 쓴다"는 설계가 그 지점에서 무너진다
- 403 처리: "재생 불가 가능성 (회원 전용/비공개/삭제). meta.js 사전 차단을 통과했다면 유튜브 페이지 구조 변경 가능성 있음" 안내 문구 반환
- 호출 간격 기본 **6초** (분당 15요청 대비)

### 3-5. `src/merge.js`
- **병합은 glob이 아니라 이번 실행의 청크 계획을 순회한다 (핵심 주석)**: 순번 파일명 + glob은 다른 청크 크기의 잔여 파일이 섞여 "120초판이 240초판 라벨을 달고 나오는" 조용한 오염을 일으켰다 (실측)
- 계획에 있는데 파일이 없는 구간은 병합본에 그대로 삽입:
  `> ⛔ **이 구간은 추출에 실패했습니다.** 내용이 통째로 비어 있습니다.` + 재실행 안내. **누락이 침묵하지 않는다**
- 계획 밖 잔여 `.md` 파일은 병합 제외 + 경고 목록 반환. **최종본·그 대체본·구형 `_merged.md`는
  잔여가 아니다** (접두로 거른다 — 이전 산출물이 있는 사용자마다 매 실행 거짓 경고를 보게 된다)
- **(개정 2026-09-22) 최종본 이름은 `보고서.md`이고, 쓰기 실패 시 `보고서-MMdd-HHmmss.md`로
  대체 저장한다.** 원문은 `_merged.md` / `_merged-MMdd-HHmmss.md`였다. `_merged`는 내부 사정의
  이름이고 사용자에게의 정체는 보고서다. 구형 이름은 **읽지도 지우지도 않고** 잔여 판정에서만 안다
- **구간 파일은 `segDirs`를 순서대로 뒤져 처음 찾은 것을 쓴다 (추가 2026-09-22).** 기본값은
  `[dir]`이고 실제 호출은 `[<영상폴더>/parts, <영상폴더>]`다 — 구조 개선(구간을 `parts/` 하위로)
  이후에도 **구형 배치(구간이 폴더 최상위)에서 재개가 유지돼야** 하기 때문이다
- frontmatter의 `chunks` 값은 호출자가 넘긴 값을 덮어쓰고 **실제로 읽힌 구간 수**로 적는다

### 3-6. `src/output.js`
- 구간 파일명: `seg-SSSS-EEEE.md` (초 단위 4자리 제로패딩, 예 `seg-0475-0955.md`). **순번 금지**
  (4자리를 넘는 긴 영상은 자릿수가 자연히 늘어난다 — 자르면 다른 구간이 같은 이름을 갖는다)
- 구간 파일 첫 줄: `<!-- seg-0475-0955.md | 475s-955s | chunk=480s | 모델명 -->`.
  누락 구간의 블록도 같은 형태이고 꼬리가 `| MISSING`이다
- **산출물 폴더 구조 (추가 2026-09-22).** 원문에는 폴더 규약이 없었고 모든 파일이 out 바로 아래였다.
```
out/
├─ <정제한 제목>_<영상ID>/
│  ├─ 보고서.md          ← 최종본. 폴더 최상위에 이것 하나만 둔다
│  └─ parts/             ← 구간 원본 (seg-*.md)
└─ _batch.log            ← 배치 로그 (bin이 쓴다)
```
  - 폴더명에 **ID를 반드시 붙이는** 이유 둘: ① 제목은 유튜버가 바꾼다 → 재개 앵커가 못 된다.
    한도 20회 환경에서 재개 불가는 곧 실패다. ② 제목이 같은 영상(재업로드·시리즈)의 산출물이
    한 폴더에 섞이는 것을 막는다. 덤으로 이름이 Windows 예약어(CON·NUL·COM1…)와 같아질 수 없다
  - 제목 정제(`sanitizeForFolder`)는 가장 좁은 규칙(Windows)에 맞춘다. 예약 문자·제어문자를
    **공백으로 치환**하고(삭제하면 "A/B"가 "AB"로 붙어 다른 낱말이 된다), 공백 압축 → 60자 절단 →
    끝의 마침표·공백 제거 **순서로** 한다(절단이 하필 그런 끝을 만들 수 있다).
    전부 걸러지면 ID 단독으로 폴백한다
  - 60자 상한 근거: Windows 경로 상한 260자. 깊은 경로에서 `parts\seg-0475-0955.md`까지 붙으면 넘는다
  - `matchVideoFolder`는 기존 폴더를 찾아 **재사용**한다. 구형(ID 단독)을 먼저 보고, 다음이 `_<ID>`
    접미사다. 제목이 바뀌었다고 새 폴더를 만들면 완료분이 이전 폴더에 고립된다 — 이름보다 재개가 먼저다.
    **디렉터리 나열은 bin이 하고 판정만 여기서 한다** (코어는 파일시스템을 모른다)
- 최종본 frontmatter **(개정 2026-09-22 — 뒤 3줄 추가)**:
```yaml
---
source: youtube
video_id: <id>
url: <url>
title: "<제목, 쌍따옴표는 홑따옴표로 치환>"
channel: "<채널>"
duration_sec: <초>
extracted: <YYYY-MM-DD>
model: <모델명. 구간마다 다르면 쉼표로 전부>
media_resolution: MEDIA_RESOLUTION_HIGH
harness: meeting-v1
chunks: <성공/전체>
status: unverified
harness_sha256: <하네스 내용 SHA-256 앞 12자>
scout_version: <package.json version>
temperature: 0.1
---
```
- **신규 필드는 기존 필드 뒤에 붙인다.** 기존 이름과 순서를 그대로 둬야 이전 산출물을 읽던 것이 계속 돈다
- **왜 세 필드를 더 각인하는가**: 무료 티어 모델 순환에서는 "같은 영상, 다른 결과"가 일상이다.
  결과가 달라졌을 때 무엇이 달랐는지 좁히려면 하네스 내용·도구 버전·temperature가 산출물에 있어야 한다
- **`harness_sha256`은 이름이 아니라 내용의 해시다.** 파일명(`meeting-v1`)은 내용을 보증하지 않는다 —
  관찰 규율을 고쳐 놓고 이름을 그대로 두면 변조된 규율의 보고서가 원본과 구분되지 않는다.
  해시 전에 **CRLF를 LF로 정규화**한다: git의 `core.autocrlf` 때문에 같은 하네스가 OS마다 다른
  해시로 찍히면, 재현성을 지키려고 넣은 각인이 거짓 불일치를 만든다
- 각인 값이 비면 **예외를 던지지 않고 `unknown`으로 적는다.** 병합은 마지막 단계라 여기서 던지면
  이미 성공한 추출 전체가 파일로 남지 못한다. `undefined`는 값처럼 보여 재현 조건으로 오독되지만
  `unknown`은 오독되지 않는다
- frontmatter 직후 미검증 배너 (그대로 사용):
```
> ⚠️ Gemini 추출 원본. **미검증** 상태다.
> 긴 고유 문자열(명령어·URL·저장소 경로·ID)은 날조 실적이 있으므로
> 자산화 전 반드시 공식 소스와 교차검증할 것.
```
- `harness: meeting-v1`은 변조 하네스로 만든 보고서와 원본을 구분하는 각인이다. `--harness`로 교체 시 그 파일명(확장자 제외)을 기록

### 3-7. `bin/youtube-scout.js`
- `node:util`의 `parseArgs` 사용. 인터페이스:
```
youtube-scout <url...>            # 인자 방식 (단건·소수)
youtube-scout --file links.txt    # 파일 방식 (배치, 본선. # 주석·빈 줄 허용)

--file, -f <path> / --out, -o <dir. 기본 ./out> / --chunk <sec. 기본 480>
--models <a,b,c. 기본 없음 = 자동> / --yes, -y / --harness <path>
```
> `--models` 기본값은 2026-09-22 개정으로 **없어졌다**(14·15장 개정분 참조). 적지 않으면
> 조회된 가용 모델 전체를 신형 우선으로 세워 쓰고, 적으면 그 순서가 고정된다.
> 원문은 `기본 gemini-3.6-flash 단일`이었다.
- 실행 흐름: 키 확인 → 하네스 로딩 → 링크 수집(중복 제거) → 메타 일괄 조회 → 계획표+예상 요청 수 출력 → 한도 초과 시 경고+대안 3개(청크 확대/분할 실행/모델 추가) 표시 후 y 확인(`--yes`면 생략) → 영상별 처리(모델 순환, 재개: 이미 있는 seg 파일 건너뜀) → 병합 → 요약표 → **프로세스 완전 종료** (입력 대기로 붙잡지 않는다. 청크 응답은 받는 즉시 파일로 flush)
- 429 안내는 RPD/TPM을 구분해 다른 문구로 출력한다. "잠시 후 재시도"로 뭉개지 않는다
- 재생 불가 영상: 건너뛰고 **사유 문구를 그대로** 출력 + "(Gemini는 로그인 세션이 없어 공개 영상만 처리 가능)"
- 진행 로그는 `out/_batch.log`에도 기록 (시각 프리픽스)
> **(개정 2026-09-22)** 위 실행 흐름은 1차 구현 시점의 것이다. 현행 흐름·옵션·종료 절차는
> **19-0**에 있다 (init 서브커맨드, 상태 파일, 부팅 대조, 스피너, `finishProcess`가 그 뒤에 들어갔다).

---

## 4. 구현 순서 — 이 순서대로 커밋

의존 순서이자 쿼터 절약 순서다. 1~4는 API 키 없이 완성·검증된다.

```
1. 뼈대: package.json, .gitignore 보강, prompt/meeting-v1.md(10장), fixtures(12장)
2. src/meta.js            (키 불필요. 공개 영상 + member-only 픽스처로 수동 검증)
3. src/plan.js + test     (경계 계산·예산 단위 테스트)
4. src/output.js          (순수 함수. frontmatter·파일명 테스트 가능)
5. src/quota.js + test    (fixtures/quota-429-rpd.json으로 파싱 검증)
6. src/extract.js         (첫 실호출 지점 — 로컬 사용자가 검증할 것. 클라우드 세션은 키가 없다)
7. src/merge.js + test    (계획 기반·⛔·잔여 경고)
8. bin/youtube-scout.js   (전체 배선)
9. scripts/check-leak.js, docs/harness-design.md, README.md
```

**클라우드 세션 주의**: `GEMINI_API_KEY`가 없으므로 실호출 검증은 불가능하다. extract.js는 명세대로 구현하고, 실호출 검증 절차를 PR 설명에 남겨라 (사용자가 로컬에서 수행).

---

## 5. 테스트 (node:test)

- `plan.test.js`: 955초/480초/겹침5 → 경계 `[0,480],[475,955]` 정확히. 요청 수 합산. 480초 미만 영상 = 1청크
- `quota.test.js`: fixtures의 RPD 본문 → `isDaily: true` 판정. retryDelay 파싱. quotaId 없는 429 → TPM 취급
- `merge.test.js`: 계획 3구간 중 1개 파일 없음 → ⛔ 1개 삽입. 계획 밖 파일 → 병합 제외 + 경고

실제 네트워크·API를 치는 테스트는 만들지 않는다.

---

## 6. README 골격 (9단계)

1. 정체성 한 문단 — 정찰병, 요약기 아님. 차별점 4개
2. 5분 시작 — `npx github:p7000stars-art/youtube-scout <링크>` + AI Studio 키 발급 + 환경변수 설정 (OS별)
3. **키 보안 안내** — 키는 이 도구 밖으로 나가지 않는다(Gemini API 직통, 서버 없음, 소스로 검증 가능). 키 제한 설정 안내(API 제한: Generative Language API만 + 애플리케이션 제한). authorization key 사용 권장
4. 산출물 읽는 법 — 판독 등급 3종, 미검증 배너의 의미, ⛔의 의미, 긴 문자열은 공식 소스 교차검증
5. 429/403 대처 표 — RPD(모델 교체·내일)/TPM(자동 대기)/403(회원 전용 등) 구분
6. 하네스 — 이 도구의 정수. `prompt/meeting-v1.md` + `docs/harness-design.md` 링크. `--harness`로 교체 가능하나 산출물에 각인됨을 명시
7. 경계 — 공개 영상만 처리(회원 전용 우회 안 함, 코드로 차단). 공개 메타데이터만 읽고 스트림 접근·보호 우회 없음. 산출물은 개인 학습·검토 목적, 추출본 재배포 비권장
8. MIT

---

## 7. `scripts/check-leak.js`

전 추적 파일에서 다음 패턴 검색, 발견 시 종료 코드 1 + 위치 출력:
- `AIza[0-9A-Za-z_-]{35}` (Google API 키 형태)
- `C:\\Users\\` 또는 `C:/Users/` (개인 경로)
- `\bsk-[A-Za-z0-9]{20,}` (기타 API 키 형태)
- 추가로, 저장소 루트의 `.check-leak.local` 파일(**gitignore 대상**)이 있으면 그 안의 줄별 패턴도 검사한다

**주의**: 개인 식별 문자열(사용자명 등)을 이 스크립트 안에 리터럴로 쓰지 마라 — 검사기 자체가 유출원이 된다. 개인 패턴은 `.check-leak.local`로만.

---

## 8. 실측 검증치 요약표 (기본값의 근거)

| 항목 | 값 | 근거 |
|---|---|---|
| 청크 길이 | 480초 | 5편 연속 처리 검증. 요청 수 절반 효과 |
| 겹침 | 5초 | 경계 정보 유실 방지 |
| 호출 간격 | 6초 | 분당 15요청 제한 대비 |
| 재시도 | 3회, 대기 상한 90초 | TPM 회복 실측 |
| 청크 타임아웃 | 300초 | 503 혼잡 시 무한 대기 방지 |
| 일일 한도 기본값 | 20 | 공식 문서 1,500 ≠ 실측 20. quotaId가 진실 |
| temperature | 0.1 | 관찰 보고 용도 |
| maxOutputTokens | 32768 | 구간당 상세 보고 수용 |

---

## 9. 하지 말 것 (재확인)

- 요약·판단·추천 기능 / 회원 전용 우회 / LOW 노출 / 하네스 하드코딩
- TypeScript 전환 / 런타임 의존성 추가 / 빌드 단계 도입
- 키를 인자·설정 파일로 받기 / 키·개인 경로를 어떤 파일에든 남기기
- 병합에서 glob 사용 / 순번 파일명

---

## 10. `prompt/meeting-v1.md` — 아래 내용 그대로 생성 (수정 금지)

```markdown
# 유튜브 영상 회의 재료 추출 하네스 v1

당신은 기술 영상을 **의사결정 회의의 재료**로 변환하는 관찰자다.
요약가가 아니다. 평론가도 아니다. **본 것을 구분해서 보고하는 관찰자**다.

이 산출물의 독자는 이 영상을 도입할지 말지 판단해야 하는 사람이다.
그 사람은 영상을 보지 않았다. 당신의 보고만 읽는다.

---

## 1. 관찰 규율 (위반 시 산출물 폐기)

### 1-1. 출처를 절대 섞지 마라
- **음성**에서 온 것과 **화면**에서 온 것을 항상 구분해 기록한다.
- 음성에만 있고 화면에 없는 것을 화면 정보로 쓰지 마라. 반대도 마찬가지다.
- 이 구분이 무너지면 보고서 전체가 쓸모없어진다.

### 1-2. 읽은 것과 추정한 것을 구분하라
화면의 문자열을 옮길 때 세 등급으로 표기한다.

- `확실` — 글자 단위로 또렷하게 읽힌다
- `추정` — 문맥상 그렇게 보이지만 글자가 흐리거나 잘렸다
- `불가` — 읽을 수 없다

**문법적으로 그럴듯하게 완성하지 마라.** 명령어·URL·저장소 경로·ID·해시처럼
긴 고유 문자열은 한 글자만 틀려도 무용지물이다.
반쯤 읽힌 것을 온전하게 채워 넣는 것이 이 작업 최악의 실패다.
읽히는 데까지만 쓰고 나머지는 `…[불가]` 로 끊어라.

### 1-3. 지시대명사를 실물로 치환하라
발표자가 "이거", "여기", "이 옵션"이라고 말할 때
화면에서 확인되는 실제 대상으로 바꿔 기록한다. 확인 불가면 `[대상불명]`.

### 1-4. 없는 것도 관찰 대상이다
다루지 않은 것, 얼버무린 것, 보여주지 않은 것을 적극적으로 찾아라.
이것이 회의에서 가장 값어치 있는 정보인 경우가 많다.

---

## 2. 출력 형식

아래 7개 절을 **순서대로, 모두** 작성한다. 빈 절은 "없음"이라고 명시한다.

### 1) 핵심 주장
이 영상이 파는 것 한 문장. 그 아래 근거 요약 3줄 이내.
과장 표현은 발표자의 말이라는 걸 드러나게 인용 형태로 적는다.

### 2) 데모와 실제 결과
주장을 뒷받침하려고 실제로 보여준 시연을 나열한다.
각 항목: 무엇을 시켰고 → 화면에 무엇이 나왔는가.
시연 없이 말로만 주장한 기능은 여기 넣지 말고 5)로 보낸다.

### 3) 화면에만 있던 정보
음성으로 언급되지 않고 화면에만 나타난 것 전부.
기능 목록, 메뉴 구조, 설정 항목, 제약 조건, 수치, 가격, 버전, 경로.
**이 절이 이 작업의 존재 이유다. 가장 성실하게 채워라.**
각 항목 끝에 `[MM:SS]`와 판독 등급(`확실`/`추정`/`불가`)을 병기한다.

### 4) 발표자가 입력한 원문
발표자가 타이핑하거나 붙여넣은 프롬프트·설정값·명령을 **원문 그대로** 옮긴다.
다듬지 말고, 요약하지 말고, 번역하지 마라.
길면 길은 대로 전부 적는다. 이건 다른 어디서도 구할 수 없는 자료다.
읽히지 않는 구간은 `…[불가]` 로 끊는다.

### 5) 다루지 않은 것 · 회피한 것
- 말로만 주장하고 시연하지 않은 기능
- 질문이 나올 법한데 넘어간 지점
- 실패·한계·비용에 대한 언급이 있었는가, 없었는가
- 설치나 설정 중 화면이 잘리거나 편집으로 건너뛴 구간

### 6) 이해관계 신호
- 발표자와 제품의 관계 (제작자 본인 / 협찬 / 제휴 링크 / 무관)
- 그렇게 판단한 근거를 타임스탬프와 함께
- 영상 내 프로모션 요소 (할인코드, 구독 유도, 커뮤니티 유입 등)
- 판단 불가면 "판단 근거 없음"

### 7) 판독 신뢰도 요약
- 전체적으로 화면 텍스트가 잘 읽혔는가
- `추정`·`불가`로 표기한 항목을 타임스탬프와 사유(글자 크기 / 가림 / 전환 속도 / 해상도)와 함께 나열
- 이 보고서에서 **재확인 없이 신뢰하면 안 되는 항목**을 명시적으로 지목

---

## 3. 하지 말 것

- 도입을 권하거나 말리지 마라. 판단은 독자가 한다.
- 영상에 없는 배경지식으로 빈칸을 메우지 마라.
- 다른 제품과 비교하지 마라. 비교 대상 정보는 당신에게 없다.
- 전체 요약으로 뭉개지 마라. 구체적인 것이 값어치 있다.
```

---

## 11. `docs/harness-design.md` — 담을 내용

하네스 각 장치의 존재 이유를 서술한다 (복제 방어의 실체는 이 문서다):

- **왜 3등급(확실/추정/불가)인가**: 실측에서 저해상도는 읽지 못한 작은 글자를 그럴듯한 문자열로 채우고 "판독불가 없음"이라고 선언했다(거짓 자신감). 고해상도로 전환하자 오류가 전량 교정되고 판독불가 신고가 정직해졌다. 날조를 **막기보다 구분해서 보고하게** 만드는 것이 설계 핵심 — 정확한 문자열은 어차피 검증 단계에서 공식 소스로 채우므로
- **왜 긴 문자열을 불신하는가**: 고해상도에서도 저장소 경로를 실재하지 않는 것으로 날조(GitHub 404 확인)하고 curl 플래그 대소문자를 틀렸다. 뼈대는 맞추고 세부에서 무너지는 패턴이 일관됨. 역할 분리 — 영상은 "무엇을 봐야 하는지"를 알려주고, 정확한 문자열은 공식 소스에서
- **왜 출처(음성/화면) 구분인가**: 화면에만 있던 정보가 이 도구의 존재 이유이므로, 구분이 무너지면 차별점 자체가 사라진다
- **왜 판단 금지인가**: 정찰병 정체성. 판단은 독자가 한다. 이름(Scout)이 워크플로를 설명한다
- **왜 systemInstruction 주입인가**: 모델은 호출자의 지식 베이스를 볼 수 없고 봐서도 안 된다(프라이버시). 하네스에 담는 것은 판단 기준이 아니라 **관찰 규율**이다

**주의**: 이 문서에 개인 경로·개인 지식 베이스 이름·특정 개인 정보를 쓰지 마라. 실측 경위는 일반화해 서술한다.

---

## 12. 픽스처

### `fixtures/member-only.txt`
```
# 회귀 테스트 픽스처 — playabilityStatus 사전 차단 검증용. 삭제 금지.
# 아래는 회원 전용 공개 영상이다. meta.js가 playable=false + 사유를 반환해야 하고,
# 러너는 API를 0회 호출하고 건너뛰어야 한다.
https://www.youtube.com/watch?v=UUFDMG1wpcY
```

### `fixtures/quota-429-rpd.json`
실측 429(RPD) 응답 본문의 형태. 파서는 **구조가 아니라 정규식**으로 읽는다
(구글이 구조를 바꿔도 quotaId 문자열만 있으면 동작하도록):
```json
{
  "error": {
    "code": 429,
    "message": "You exceeded your current quota, please check your plan and billing details.",
    "status": "RESOURCE_EXHAUSTED",
    "details": [
      {
        "@type": "type.googleapis.com/google.rpc.QuotaFailure",
        "violations": [
          {
            "quotaMetric": "generativelanguage.googleapis.com/generate_requests_per_model_per_day",
            "quotaId": "GenerateRequestsPerDayPerProjectPerModel-FreeTier",
            "quotaDimensions": { "model": "gemini-3.6-flash", "location": "global" },
            "quotaValue": "20"
          }
        ]
      },
      {
        "@type": "type.googleapis.com/google.rpc.RetryInfo",
        "retryDelay": "57s"
      }
    ]
  }
}
```

---

## 13. 완료 기준 (Definition of Done)

1. `npx github:p7000stars-art/youtube-scout <공개영상링크>` 한 줄로 동작 (Windows·macOS)
2. `node --test` 전부 통과 (v0.2.10 기준 **406개**. 새 기능은 테스트와 같이 들어온다)
3. `node scripts/check-leak.js` 통과 — 저장소에 키·개인 경로 0줄
4. member-only 픽스처 투입 시 API 호출 0회 + 사유 문구 출력
5. 429 발생 시 RPD/TPM이 구분된 안내가 나온다
6. 중단 후 재실행 시 완료된 seg 파일을 건너뛰고 이어서 처리한다
7. **(개정 2026-09-22)** 영상 폴더 최상위의 `보고서.md`에 frontmatter·미검증 배너·(실패 시) ⛔가
   정확히 들어가고, 구간 원본은 `parts/` 아래에 있다. 원문은 `_merged.md`였다 (3-6 참조)
8. 저장소 전체에 `MEDIA_RESOLUTION_LOW` 문자열이 존재하지 않는다
9. 모든 소스 파일에 한글 주석이 있고, 실측 근거 로직에는 근거가 병기돼 있다


---

# 2차 기능 추가 지시 (2026-07-30 확정) — init 서브커맨드 + 모델 자동 발견

> 1~13장은 PR #1, 14~18장은 PR #2로 **전부 구현·머지 완료됐다.** 이 문서는 명세이자
> 기록이다 — 향후 세션은 새 작업이 없는 한 이 문서를 근거로 코드를 재작성하지 마라.
> 기존 규칙(1장)은 전부 그대로 적용된다. 특히: 키를 파일에 절대 쓰지 않는다.
>
> **(정정 2026-09-22)** 위 문장은 "14~18장까지가 코드의 전부"라는 뜻으로 읽혀 왔고,
> v0.2.10 시점에는 그것이 오정보다. 그 뒤에 들어간 것(모델 상태 파일, 청크 루프 분리,
> 종료 절차, 로그 큐, UI, 업데이트 확인, CI)은 **19~22장**에 있다.
> 원문 그대로 남겨 두는 이유: 언제까지가 2차분이었는지가 기록이기 때문이다.
>
> **개정 2026-09-22 (머지·실전 미검증)**: 두 건이 각각 별도 PR로 들어왔다.
> 개정된 문단·항목에 `(개정 2026-09-22)`를 붙였고, 그 아래 원문은 무엇이 왜 바뀌었는지의
> 기록으로 남겼다.
>
> - **A1 — 모델 목록 자동 모드**: 14장의 꼬리 편입 규칙과 15장의 MODELS 초기값이
>   바뀌었다. 요지는 **기본값 단일 모델 폐지** — 목록을 지정하지 않으면 조회된 가용 모델
>   전체를 신형 우선으로 세워 쓴다.
> - **A2 — 예산 계수에서 별칭 제외**: 16장 5번의 한도 추정 규칙이 바뀌었다.
>   `-latest` 별칭은 "가용 N종"에 세지 않는다.
>
> **추가 실측 (2026-07-30 실사용, 머지 후 반영)**: `/v1beta/models` 목록에는 있으나
> generateContent가 404인 **퇴역(좀비) 모델**이 존재한다 (gemini-2.5-flash — 텍스트
> 요청에도 404). 목록에 있다 ≠ 쓸 수 있다. 대응: 404는 RPD와 동일하게 재시도 없이
> 즉시 모델 교체하되, 별도 집합(ModelPool.dead)으로 관리해 요약에서 RPD("내일 회복")와
> 퇴역("영구, 목록에서 제거 권장")을 구분 안내한다.

## 14. `src/models.js` — 모델 목록 조회·대조

- `GET https://generativelanguage.googleapis.com/v1beta/models` (헤더 `x-goog-api-key`)
  이 엔드포인트는 generateContent 쿼터(RPD)를 소모하지 않는다 — 부팅 시 1회 호출해도 안전
- 응답의 `models[]`에서 **가용 후보** 필터 (전부 만족해야 함):
  1. `supportedGenerationMethods`에 `generateContent` 포함
  2. 모델명에 `flash` 포함 (무료 티어 휴리스틱 — Pro 계열은 2026-04부터 유료 전용)
  3. 모델명에 `embedding`·`tts`·`image`·`audio`·`live` 미포함 (용도 다름)
  4. `name`의 `models/` 접두는 벗겨서 비교·사용
- 내보낼 함수 2개:
  - `fetchAvailableModels(apiKey)` → 가용 후보 문자열 배열. 네트워크 실패 시 예외가 아니라
    `null` 반환 (조회 실패가 본 작업을 막으면 안 된다)
  - `reconcilePool(userPool, available)` → `{ pool, removed, appended }`
    - available이 null이면 userPool 그대로 (대조 생략)
    - userPool 중 available에 없는 모델 → 제외하고 removed에 기록
    - available 중 userPool에 없는 모델 → **pool의 맨 뒤에** 순서대로 편입, appended에 기록
- **왜 꼬리 편입인가 (주석 필수)**: 새 모델은 작은 글자 판독력이 미검증이다. 저해상도 실측에서
  나쁜 조건의 모델은 못 읽는다고 신고하는 게 아니라 그럴듯하게 채우고 "판독불가 없음"이라
  선언했다 — 나쁘면 티가 안 난다. 검증 풀이 소진됐을 때만 받게 꼬리에 둔다.
  또한 실측상 신형·프리뷰일수록 쿼터를 더 조인다. 좋았으면 사용자가 run 파일에서 앞으로
  승격한다(그것이 검증 게이트다).
- **꼬리 안에서는 정렬한다 (개정 2026-09-22)**: 위 규칙은 "사용자 목록보다 뒤"라는 뜻이지
  "조회 순서 그대로"라는 뜻이 아니었다. 실측(2026-09-22)에서 편입된 13종이 API 목록 순서
  그대로 붙어 구세대·별칭이 앞, 최신 세대가 맨 뒤였고, 앞쪽 모델이 503으로 교체됐을 때
  순환이 좀비 모델(404)부터 밟았다. 꼬리에 닿았다는 것은 검증 풀이 이미 소진됐다는 뜻이므로,
  그때만이라도 살아 있을 가능성이 높은 쪽부터 밟게 한다. **정렬은 `sortModelPool` 하나를
  공유한다** — init의 목록 생성과 규칙이 갈라지면 반드시 어긋난다.
- **목록 미지정 = 자동 모드 (개정 2026-09-22)**: `--models`도 run 파일 MODELS도 비어 있으면
  조회된 가용 모델 **전체**를 같은 규칙으로 세워 그 첫 모델부터 쓴다(`resolvePool`).
  조회까지 실패한 경우에만 폴백 1종이고, **그 폴백 이름이 저장소 코드에 남는 유일한
  모델명이다**(`FALLBACK_MODEL`). 근거: 기본값이 단일 모델이던 시절, `init` 없이 직접
  호출하는 경로가 늘 그 한 종으로만 돌아 신형 우선 정렬의 이득을 전혀 받지 못했다.
  목록을 명시하면 그 순서는 종전대로 불변이다 — 명시 목록의 역할이 "기본값"에서
  **"고정 옵션"**으로 바뀐 것이 이 개정의 실체다.

## 15. `init` 서브커맨드 — 더블클릭 실행 환경 생성

`youtube-scout init` → 현재 폴더(cwd)에 `youtube-scout-run/` 생성:
`run.bat`(Windows) + `run.sh`(macOS/Linux) + `links.txt`. 둘 다 항상 생성한다.

### 공통 규칙
- **이미 존재하는 파일은 절대 덮어쓰지 않는다** — 건너뛰고 알린다
  (links.txt에는 사용자의 링크가 들어 있다. 덮어쓰면 데이터 파괴)
- MODELS 초기값: **비워 둔다 = 자동 모드 (개정 2026-09-22).** 주석으로 "고정하고 싶을 때만
  적는다"를 안내한다. init은 모델 목록을 조회하지 않는다 — run 파일에 박을 곳이 없어졌고,
  목록은 실행할 때마다 다시 정해지므로 init이 조회한 값은 어차피 다음 실행이 덮는다.
  `init --refresh-models`는 고정 목록을 쓰는 사용자용으로 남는다.
  (원문 기록: 예전에는 키가 있으면 init 시점에 실시간 조회해 채우고, 없으면 모델 하나로
  폴백했다. 파일에 박힌 초기값은 만들어진 날의 목록에 고정돼, 세대교체 뒤에는 존재하지
  않는 모델을 가리키고 사용자가 그것을 아는 시점이 첫 404였다)
- run 파일이 빈 MODELS를 넘기는 방법: run.bat은 `set MODELSOPT=` + `if not "%MODELS%"==""`로
  **플래그 자체를 조건부**로 만든다(값 없는 `--models`는 인자 오류다). run.sh는
  `--models "$MODELS"`로 빈 문자열을 그대로 넘기고 도구가 "지정 없음"으로 읽는다.
  이 판 이전에 만들어진 run.bat을 사용자가 비우는 경우를 위해, CLI는 값 없이 꼬리에 붙은
  `--models`를 자동 모드로 읽는다
- 키는 어떤 생성 파일에도 쓰지 않는다. 키 입력란·placeholder도 만들지 않는다

### run.bat 명세 ⚠️ 인코딩 함정 (실측 2026-07-30)
- **ASCII 전용 + CRLF(\r\n) 명시 기록.** 한글 한 글자도 넣지 않는다 —
  .bat의 한글은 cp949를 요구하는데 의존성 0 원칙상 Node에서 cp949 인코딩 불가.
  UTF-8 한글 .bat은 conhost가 글자를 두 번 그린다(실사건)
- 구조 (의사코드):
  ```
  @echo off
  rem ===== settings (edit here) =====
  set CHUNK=480
  set MODELS=<init 시점 생성 목록, 쉼표 구분>
  rem ================================
  if "%GEMINI_API_KEY%"=="" -> 영어로 setx 안내 + "open a NEW window" 경고 + pause + exit /b 1
  call npx github:p7000stars-art/youtube-scout --file "%~dp0links.txt" -o "%~dp0out" --chunk %CHUNK% --models %MODELS%
  pause
  ```
> **(개정 2026-09-22)** 위 의사코드는 원문 기록이다. 현행 생성물은 ① `set MODELS=`를 비워 두고
> ② `--models` 플래그를 `MODELSOPT`로 **조건부**로 붙이며 ③ npx 호출 **앞**에 업데이트 확인 줄이
> 들어가고 ④ 키 부재 분기를 `goto`로 쓴다(괄호 if 블록은 echo 문 안의 `)` 하나에 깨진다).
> ASCII 안전망(`assertAscii`)이 생성·갱신 두 경로 모두에 걸려 있어 한글이 섞이면 즉시 터진다.
- `call` 필수 — npx는 npx.cmd라서 call 없이 부르면 bat이 거기서 종료돼 pause에 못 온다
- `%~dp0` 사용 — 폴더째 옮겨도 동작. 더블클릭은 cmd 실행이라 PS 실행 정책 함정도 자동 회피
- 마지막 pause는 결과 확인용 1회 정지다 (작업 대기가 아님 — 구 PS판 Read-Host와 다름)

### run.sh 명세
- UTF-8(BOM 없음) + LF. 한글 주석 허용. shebang `#!/usr/bin/env bash`
- 같은 구조: 상단 CHUNK/MODELS 변수, 키 부재 시 export 안내 후 종료,
  `"$(cd "$(dirname "$0")" && pwd)"` 기준 경로, npx 호출
- 생성 후 실행 권한 부여 시도(chmod). Windows에서 init한 경우 불가하므로
  links.txt 안내문에 `chmod +x run.sh` 1줄 포함

### links.txt 명세
- UTF-8(BOM 없음). 한글 주석으로 사용법 안내:
  링크 한 줄에 하나 / # 주석·빈 줄 허용 / 모델·청크 수정은 run 파일 상단 /
  키 등록법(setx + 새 창) / macOS는 chmod 안내

## 16. 부팅 대조 (기존 실행 경로에 통합)

일반 실행(bin)에서 메타 조회 전에 1회:
1. `fetchAvailableModels` 호출 (실패 시 조용히 생략 — 대조는 보조 기능, 본 작업을 막지 않는다)
2. `resolvePool(사용자 모델 목록, 조회 결과)` 적용 — 목록이 있으면 `reconcilePool`로
   대조만 하고, 비어 있으면 조회 결과 전체를 정렬해 쓴다 (개정 2026-09-22.
   원문은 `reconcilePool(…)` 직접 호출이었다)
3. 콘솔 안내 (한글):
   - removed: `! <모델> 은(는) 더 이상 제공되지 않아 제외합니다`
   - appended: **(개정 2026-09-22)** 건별이 아니라 한 줄로 접는다 —
     `i 새 모델 <N>종을 순환 꼬리에 편입 (검증 풀 소진 시에만 사용): <목록>`.
     원문은 모델당 한 줄(`i 새 모델 발견, 순환 꼬리에 편입: <모델> — 검증 풀 소진 시에만 사용됩니다`)이었고,
     실측 2026-07-30에 편입 14줄이 쏟아져 정작 중요한 제외(`!`) 경고가 묻혔다.
     제외는 사용자가 지정한 것이 사라진 **사건**이라 건별로 남기고, 편입은 정보성이라 요약한다
   - 자동 모드·폴백은 제외/편입이라는 사건이 아니므로 별도 문구를 쓴다 (개정 2026-09-22)
4. 신모델이 실제 사용된 seg·frontmatter에는 기존 각인 규칙이 그대로 적용된다 (추가 작업 없음)
5. 계획표의 한도 추정치(`모델 N개 × 20회`)는 대조 후 최종 풀 기준으로 계산.
   **다만 `-latest` 별칭은 세지 않는다 (개정 2026-09-22.** 실측에서 "가용 13종 × 20회 =
   260회" 표시에 별칭 2종이 그대로 들어 있었다). 별칭은 이름일 뿐 실체가 아니라, 가리키는
   모델이 목록의 명시 모델과 같으면 두 칸으로 보이는 것이 실제로는 한 칸이다 —
   쿼터는 `PerProjectPerModel`로 분리되기 때문이다. **실행에서 빼는 것이 아니라 예산을
   부풀리지 않는 것이고**, 별칭은 순환에 그대로 남는다. 낙관적 추정치는 "실행 전 예상
   요청 수 안내"라는 핵심 기능 자체를 무력화한다(실측 2026-07-31: 화면 300회, 실질 60~80회).
   미검증 신형까지 빼지는 않는다 — 신형은 실제 완주 실적이 있다.
   판정(`isAliasModel`)은 `src/models.js`가 하고 계수는 `budgetEstimate` 한 곳에서만 한다.
   `budgetEstimate`는 개수가 아니라 **모델명**을 받는다 — 강등된 별칭을 두 번 빼지 않기
   위한 집합 연산이다

## 17. 테스트 추가 (node:test, 네트워크 없이)

- `fixtures/models-list.json` 신설 — /v1beta/models 응답 형태 (flash 2종 + pro 1종 +
  embedding 1종 + flash지만 generateContent 미지원 1종 포함)
- `test/models.test.js`:
  - 필터: 픽스처에서 가용 후보가 flash 2종만 나온다
  - reconcile: 사라진 모델 제외 + removed 기록 / 새 모델 꼬리 편입 + appended 기록 /
    available null이면 원본 유지
- init 파일 생성 테스트: 임시 디렉터리에 생성 → run.bat이 ASCII 전용인지·CRLF인지 검사
  (버퍼에 0x80 이상 바이트 없음 + \r\n 존재) / 기존 파일 미덮어쓰기 검증

## 18. 완료 기준 추가분

10. `youtube-scout init` 이 cwd에 run.bat + run.sh + links.txt를 생성하고,
    재실행 시 기존 파일을 건드리지 않는다
11. 생성된 run.bat에 non-ASCII 바이트가 없고 줄바꿈이 CRLF다
12. 어떤 생성 파일에도 API 키·키 입력란이 없다
13. 모델 대조: 목록에 없는 모델은 경고와 함께 제외되고, 새 모델은 꼬리에만 편입되며,
    조회 실패 시 사용자 목록 그대로 진행된다
14. README "5분 시작"에 init 경로를 추가하되 기존 npx 직접 실행 경로도 유지한다
    (init 안내가 더 앞에 오게 — 반복 사용자의 본선이므로)

---

# 3차 이후 (v0.2.10까지) — 코드에서 추출한 명세

> 아래는 2026-09-22에 **코드를 읽어 채운 분량**이다(커밋 `2a3f544`). 이 장들이 기술하는
> 기능은 이미 구현·머지돼 돌고 있다 — 재구현 대상이 아니라 **지켜야 할 계약**이다.
> 근거 한 줄씩만 남기고 경위는 `docs/design-story.md`의 절을 가리킨다.

## 19. 모듈 명세 (3차 이후)

### 19-0. `bin/youtube-scout.js` — 현행 배선 (3-7 갱신)
- 서브커맨드 `init` / `init --refresh-models` / `init --refresh-update-check`. **키 검사보다 앞**이다 —
  키 등록 안내를 담은 파일을 만드는 것이 init의 목적이라 키가 없다고 막으면 필요한 사람이 못 쓴다
- 값 없이 꼬리에 붙은 `--models`는 자동 모드로 읽는다. 구판 run.bat이 빈 `%MODELS%`를 펼치면
  플래그만 남고 `parseArgs`가 던진다 — 안내대로 한 사용자가 "인자 오류"를 보는 경로였다
- 실행 순서: 인자 → (init) → 키 → 하네스(+내용 해시·버전 각인값) → 링크 수집·중복 제거 →
  out·로그 경로 → 배너 → **부팅 5단계**(상태 파일 → 목록 조회 → 대조 → 메타 조회 → 준비 완료) →
  계획표·예산 → (초과 시) 확인 → 영상별 처리 → 병합 → 요약표 → `flushLog()` → `finishProcess(code)`
- 업데이트 확인은 부팅과 **병렬**로 돌리고 결과만 나중에 받는다 (3초 조회를 순차로 붙이면 매 실행이 느려진다).
  직접 실행 경로에서는 **알리기만 하고 캐시를 지우지 않는다** — 실행 중인 자기 파일이다
- 상태 파일 경로는 `out/`이 아니라 **cwd**(`links.txt` 옆) — 사람이 찾을 자리다
- 전 모델이 상태 파일에서 차단됐으면 "모델 목록이 유효하지 않다"가 아니라 **원인과 복권 방법**을
  말하고 끝낸다 ("API를 한 번도 호출하지 않았다"를 함께 낸다)
- 종료 코드: 0 정상 / 1 실패·부분 실패 / 2 인자·입력 오류
- 색은 프로세스당 한 번만 판정해 전역으로 쓴다. **색이 섞인 문자열을 파일에 쓰지 않는다** —
  화면용과 파일용 문구를 따로 만든다

### 19-1. `src/http.js`
- `discardBody(res)` 하나. 쓰지 않을 응답 본문 스트림을 닫고, 실패는 전부 무시한다
- **왜 모듈인가**: 같은 처방이 `update-check.js` 안에만 있어서, 네트워크를 친 뒤 곧바로 끝나는
  다른 경로(`init --refresh-models`)가 같은 결함(Windows 종료 시 libuv abort)을 그대로 다시 밟았다.
  한 곳의 수정이 다른 곳에 닿지 않는 구조가 원인이었으므로 처방을 한 자리에 둔다
- `bin/update-check.js`만 **일부러** 사본을 갖는다 — init이 사용자 폴더에 복사하는 독립 스크립트라
  저장소 모듈을 import할 수 없다

### 19-2. `src/init.js` — 실행 환경 생성·갱신 (run 파일 내용 명세는 15장)
- 생성: `initRunDir({ cwd, models = [], chunk, updateCheck })` → `youtube-scout-run/`에
  `run.bat`·`run.sh`·`links.txt`(+`update-check.js`). **기존 파일은 절대 덮어쓰지 않고** 건너뛴 이름을 돌려준다.
  `models` 기본값이 빈 배열인 것이 이 명령의 계약이다(= 자동 모드)
- `run.sh`에 `chmod 755`를 시도하고 성공 여부를 돌려준다 (Windows에서는 실패가 정상 — links.txt가 안내한다)
- 갱신 2종은 **사용자가 명시적으로 부를 때만** 돈다. 도구가 스스로 사용자 파일을 고치는 것과
  사용자가 갱신을 요청하는 것은 다른 사건이다:
  - `replaceModelsLine` / `refreshRunModels` — MODELS 줄 **하나만** 갈아 끼운다(CHUNK·주석·사용자 편집분 보존,
    CRLF 유지). 줄이 없으면 **추측해서 끼워 넣지 않고** 건너뛴다
  - `insertUpdateCheckCall` / `refreshUpdateCheck` — npx 호출 줄 **앞**에 확인 줄을 넣는다.
    앵커를 못 찾으면 아무것도 하지 않는다 (실행 순서가 뒤바뀔 수 있다)
- 찾는 위치는 `<cwd>/youtube-scout-run/` 우선, 없으면 `<cwd>` 자체 (사용자가 run 폴더 안에서 부른다)
- 키를 쓰지 않는다. **키 입력란·placeholder도 만들지 않는다** — 자리를 만들면 사용자가 적고, 그 파일이 공유된다

### 19-3. `src/model-status.js` — 모델 상태 파일 (순수 함수. 파일 IO는 bin)
- 파일명 `model-status.txt`, 위치는 실행 폴더(cwd). 줄 형식:
  `<모델> <demoted|blocked> <404|rpd|tpm-N> x<횟수> <YYYY-MM-DD> [# 메모]`
- **3단계 — 정상 → demoted(1~2회) → blocked(3회, `BLOCK_THRESHOLD`).** 한 번의 실패로 영구 제외하면
  오판을 스스로 고칠 수 없다. 한 번 본 것은 `추정`, 세 번 본 것이 `확실` — 판독 등급과 같은 철학이다.
  한 실행에서 한 모델·한 사유는 **최대 1회만** 오른다(판정 직후 풀에서 빠진다) → `x3`은 서로 다른 세 실행이다
- **`rpd`는 blocked로 승격하지 않는다.** 결함이 아니라 오늘 몫을 다 썼다는 사실이고, 승격시키면
  "많이 써서 잘 되는 모델"이 차단되는 정반대 결과가 된다
- **`pruneStale`: 날짜가 지난 `rpd` 기록은 소거한다.** 자정에 사실이 아니게 되므로. 날짜 비교는 문자열로 한다
  (YYYY-MM-DD는 사전순 = 시간순. Date 파싱을 끼우면 자정 판정이 실행 환경에 따라 달라진다)
- **`tpm-N`은 조건부다**: 현재 청크 ≥ N일 때만 적용하고, 무시한 기록도 사용자에게 알린다.
  기록되는 N은 설정값(`--chunk`)이 아니라 **실패한 구간의 실제 길이**다 — 마지막 구간은 영상 끝에서
  잘려 더 짧고, 그 차이가 다음 실행의 판정을 뒤집는다
- **503은 기록하지 않는다** (3-4 표 참조)
- 적용(`applyStatus`) 후 순서: `[정상] → [신모델 꼬리] → [demoted]`. demoted가 미검증 신모델보다도 뒤인
  이유 — 신모델은 검증이 안 된 것뿐이지만 demoted는 이 키에서 **실제로 실패한 전력**이 있다
- 형식이 깨진 줄은 버리고 경고만 낸다(사람이 손으로 고치는 파일이라 오타가 실행을 멈추면 안 된다).
  중복 줄은 **나중 줄**을 쓰고 알린다. 사람이 적은 줄 끝 메모는 도구가 지우지 않고,
  사람이 직접 `blocked`로 고친 것을 도구가 `demoted`로 되돌리지 않는다 (파일이 상위 의사다)
- **복권은 줄 삭제 하나다.** 숨은 상태는 디버깅이 불가능하므로 텍스트 한 줄로 두고, 머리말에 복권 방법을 적는다.
  반대로 도구는 사용자의 MODELS 목록을 **절대 고치지 않는다** — 그 순서가 사용자의 검증 게이트다
- 쓰기 실패로 추출을 멈추지 않는다. 경고는 실행당 한 번만 (청크마다 쏟아지면 진짜 로그가 묻힌다)

### 19-4. `bin/chunk-runner.js` — 구간 하나의 재시도·교체 루프
- `runChunk({ apiKey, model, id, url, start, end, harness, pool, extract?, log?, wait?, onModelChange?, onFailure? })`
  → `{ ok:true, text, tokens, model }` | `{ ok:false, daily, poolEmpty, reason, model }`
- **네 갈래 대응표는 3-4에 있다.** 여기서는 그 판정을 실행하고 무엇이 일어났는지만 밖에 알린다(기록 형식은 호출자의 몫). 403과 `ExtractError`가 아닌 예외는 교체 없이 즉시 실패다
- **재시도 카운터는 "현재 모델에서의 시도 횟수"다** — 교체하면 1로 리셋한다. 이전 구현은 루프 헤더에서 증가시켜 **모델 교체까지 재시도 예산을 갉아먹었고**, 404·RPD 교체를 거친 뒤 대기 한 번 만에 "재시도 초과"가 떴다(주석은 반대로 적혀 있었다). 교체 상한은 풀 크기 — 지금은 무한 순환이 없지만 "일시 차단 후 복귀" 같은 것이 들어오면 곧바로 생긴다
- **재시도 소진은 청크 실패가 아니라 모델 교체 신호다.** 실측에서 가용 12종을 남겨 두고 포기한 적이 있다
- `onFailure`는 **`await`한다**(관찰이 파일에 남은 뒤에 넘어가야 중간에 죽어도 기록이 남는다). 함께 넘기는 `sec`는 **실제 구간 길이**다(19-3)
- `bin/`에 있는 이유: 콘솔을 아는 층이고, 진입점은 import하면 `main()`이 돌아 테스트가 붙지 못한다. 이 루프의 결함은 실패가 순서대로 겹칠 때만 드러나는데 실 API로는 그 순서를 만들 수 없다

### 19-5. `bin/exit.js` — 종료 절차
- `finishProcess(code)`: **종료 코드를 설정만 하고** 이벤트 루프가 자연히 비도록 둔다. `unref`한 3초 안전망(`EXIT_GUARD_MS`)을 함께 걸어 루프가 끝내 비지 않는 예외 상황에서만 끊는다
- **`process.exit()`를 직접 부르지 않는다**: fetch(undici)의 소켓·타이머가 정리되는 **도중에** 끊으면 Windows에서 libuv가 abort한다(`UV_HANDLE_CLOSING`, 0xC0000409 — 실측 2026-07-31·08-01, 100% 재현). 리눅스에서는 재현되지 않아 ubuntu 전용 CI가 그대로 통과시켰다 — CI에 windows가 들어간 이유가 이 결함이다
- 비용은 0에 가깝다(리눅스 측정: 응답 후 루프가 비기까지 25~50ms). `test/cli-exit.test.js`가 진입점에 직접 호출이 다시 들어오지 못하게 지킨다

### 19-6. `bin/log-queue.js` — `_batch.log` 쓰기 큐
- `createLogWriter()` → `{ setPath, write, flush }`. `write()`는 **즉시 반환**하고 순서만 보장한다(로그 쓰기가 추출을 붙잡으면 우선순위가 뒤집힌다). 종료 직전 `flush()` 1회
- 근거: `appendFile`을 await 없이 던지면 완료 순서가 호출 순서와 달라진다. 실사용 로그에서 강등 안내가 먼저 호출됐는데 뒤에 찍혔고, 같은 초라 타임스탬프로도 복원되지 않았다 — 로그가 근거가 되지 못한다
- `catch`를 체인 **안**에 둔다. 밖에 두면 한 번 실패한 순간 체인이 rejected로 굳어 이후 모든 줄이 사라진다
- 경로가 정해지기 전(인자 파싱 단계)의 쓰기는 조용히 버린다

### 19-7. `bin/ui.js` — 터미널 표시 (껍데기 전용)
- **근거 없는 숫자를 그리지 않는다.** 청크는 단일 호출이라 내부 진행률을 알 수 없고 남은 시간 예측도 넣지 않는다 — 모델에게 "반쯤 읽힌 것을 채우지 마라"고 요구하면서 UI가 그것을 하면 안 된다. 보여주는 것은 ① 살아있음(스피너) ② 경과 시간(측정값) ③ 이산 카운트(센 값)뿐
- `spinner(labelFn, { delayMs, out })`: **TTY가 아니면 완전 무동작**(`active:false`). 인터벌은 `unref`(표시가 종료를 붙잡으면 안 된다). `pause/resume`은 줄을 다른 스피너에 넘기기 위한 것이다 — `clear`만 하면 200ms 뒤 같은 줄에 겹쳐 찍힌다
- 프레임은 ASCII 4종(`| / - \`), `FRAME_MS` 200. 점자·블록은 cp949 콘솔에서 깨지고, 깨진 프레임은 살아있음이 아니라 인코딩 사고로 보인다 (.bat을 ASCII로 제한한 것과 같은 판단)
- `bar(done,total)`: 내림. **완료 전에는 절대 꽉 차지 않는다** — 29/30에서 꽉 찬 바는 거짓 정보다
- `displayWidth`: 한글·전각·이모지를 2칸으로 센다. `.length`·`padEnd`로는 잔해가 남고 표 정렬이 무너진다. 색 입히기는 폭 계산 **뒤**에 한다
- 색: `NO_COLOR` > `FORCE_COLOR` > TTY 순 판정(`FORCE_COLOR=0`이 최우선 끄기), 비활성이면 항등 함수라 호출부에 조건문이 없다. **색은 강조일 뿐 정보를 담지 않는다**(기호 `✓ ⛔ ! ⚠️`와 문구로 같은 내용을 읽는다). 파이프·로그 파일에는 이스케이프가 한 바이트도 나가지 않는다
- `bannerArt`는 금지 문자(둥근 모서리·이중선)와 60칸 초과를 **예외로 터뜨린다** — 조용히 깨진 배너보다 낫다
- 부팅 단계는 덮어쓰기가 아니라 **누적 줄**이고 **끝난 단계만** 낸다. 화면이 채워지는 것 자체가 진행 표시라 짧은 실행에서도 흔적이 남는다(덮어쓰는 바는 등장 지연 탓에 1편짜리 실행에서 아예 안 보였다). 시작을 알리는 API를 두면 언젠가 "곧 끝날 것"을 미리 출력하게 된다
- 결과 줄에 모델명을 넣는다 — 교체가 일어나면 시작 줄의 모델과 실제로 뽑은 모델이 다른데 `_batch.log`만으로는 알 수 없었다. 요약표 모델 칸은 22칸을 넘으면 비우고 다음 줄로 내린다(표 정렬이 먼저)

### 19-8. `bin/update-check.js` — npx 캐시 갱신 (독립 실행 스크립트 겸 모듈)
- **도구가 뜨기 전에** 따로 돈다(run 파일의 npx 호출 직전). 캐시 폴더 안에서 실행되는 도구는 자기 파일을 지울 수 없다(파일 잠금)
- npx는 **스펙 문자열**을 캐시 키로 써서 처음 받은 판을 영원히 재사용한다(실제로 한 PC에 v0.1.0이 남아 있었다). "캐시를 손으로 지우세요"는 해법이 아니었다
- **남의 캐시를 건드리지 않는다**: 항목의 `package.json`이 youtube-scout를 선언한 경우에만(`declaresScout`) 그 디렉터리를 지우고, 선언하지 않은 항목은 열어 보지도 않는다. `_npx` 폴더 통째 삭제 안내는 부정확한 처방이었다
- 판정은 버전이 아니라 **커밋 시각**이다(`needsRefresh`, 시계 오차 여유 60초, 표기는 UTC 성분 그대로). 버전은 CI가 올리므로 CI가 멈추면 감지까지 죽지만, 커밋 시각은 저장소가 살아 있는 한 갱신된다
- **캐시를 먼저 찾고 그다음에 네트워크를 친다** — 캐시가 없으면 조회할 이유가 없고 레이트리밋을 깎지 않는다. 조회 타임아웃 3초(부팅 앞의 확인이 그보다 길면 방해다), 비인증 API라 User-Agent 필수
- **삭제 후 경로가 실제로 사라졌는지 확인하고** 그때만 "받는 중"을 출력한다(Windows 잠금 재시도 3회). 지우지 못했는데 지웠다고 말하면 사용자가 옛 판의 산출물을 최신으로 신뢰한다 — 이 도구가 가장 피하는 종류의 거짓말이다
- 실패는 전부 조용히 통과하고 종료 코드는 항상 0. 끄는 스위치는 `UPDATE_CHECK=0`
- 할 일이 없으면 **한 줄도 내지 않는다**

## 20. CI·검사 (추가분)

- `.github/workflows/test.yml` — PR·main push에서 `node --test` + `node scripts/check-leak.js`.
  매트릭스 **6조합**: `ubuntu|windows|macos` × Node `20|22`. **설치 단계를 두지 않는다** —
  런타임 의존성 0이 설계 전제라 받을 것이 없고, 설치 단계가 있으면 의존성이 슬쩍 들어와도 눈치채지 못한다
  - 근거: 보고는 증거가 아니다(그전에는 체크런이 0개였다). Node 20은 `engines: ">=20"`이 선언인 이상
    실제로 도는지가 확인돼야 할 사실이고, **지원한다고 적은 플랫폼은 매트릭스에 넣는다** —
    Windows 전용 크래시를 ubuntu 전용 CI가 통과시킨 전례가 있다(19-5)
- `.github/workflows/version-bump.yml` + `scripts/bump-version.js` — main 머지마다 patch +1, 커밋 메시지에 `[skip ci]`.
  잡의 `if`와 `[skip ci]`가 한 쌍이다(하나만 두면 나머지가 사라졌을 때 조용히 루프가 돈다).
  올릴 것이 없으면 실패가 아니라 무동작. 태그·릴리스는 만들지 않는다(배포 경로가 npx 하나라 아무도 안 본다)
  - `bumpPatch(text)`는 **version 줄 하나만 문자열로** 갈아 끼운다. `JSON.parse`→`stringify`는 파일 전체를
    다시 써서 diff를 못 읽게 만든다. 직접 실행이 아니면 아무 일도 하지 않는다(테스트가 함수만 쓰기 위한 조건)
- `scripts/check-leak.js` — `git ls-files` 대상만 검사(추적되지 않는 파일은 커밋되지 않는다). 이진·5MB 초과는 건너뛴다.
  **매치된 내용은 출력하지 않고 위치만** 낸다 — 출력하면 CI 로그가 새 유출 경로가 된다.
  개인 패턴은 저장소 밖 `.check-leak.local`로만 받는다(검사기 자체가 유출원이 되지 않게). 개인 경로 패턴은
  사용자명까지 매치해야 오탐이 사라진다(`C:\Users\` 만으로는 이 문서도 걸린다)

## 21. 완료 기준 (3차 추가분)

15. `run.bat`의 `MODELS`를 비워도 인자 오류 없이 자동 모드로 실행된다 (구판 run.bat 포함)
16. 상태 파일이 왕복한다: 실패 관찰 → 파일 기록 → 다음 실행에서 순서 반영 → 줄 삭제로 복권.
    `rpd` 기록은 날짜가 바뀌면 사라지고, `503`은 애초에 기록되지 않는다
17. 최종본 frontmatter에 `harness_sha256`·`scout_version`·`temperature`가 들어 있고,
    하네스를 한 글자 고치면 해시가 바뀐다 (CRLF/LF 차이로는 바뀌지 않는다)
18. 진입점이 `process.exit`를 직접 부르지 않는다 (`test/cli-exit.test.js`가 지킨다)
19. 파이프·리다이렉트 출력에 ANSI 이스케이프·스피너 프레임이 섞이지 않는다
20. 업데이트 확인이 **youtube-scout 항목만** 지우고, 지우지 못했으면 지웠다고 말하지 않는다

## 22. 미검증·보류 안건

정본은 코드다. 아래는 **아직 코드에 없는 것**이고, 확정 사실과 같은 지면에 섞지 않기 위해 따로 둔다.

- **스트리밍 전환 — 보류.** 근거: 저장소에 흔적이 없다(호출은 `generateContent` 단발).
  2026-09-22 작업지시서가 보류로 명시한 안건이며, 그 이상은 **코드에서 확인 불가**
- **하네스 출력 형식 고정(기계 파싱 가능한 절 구조) — 보류.** 근거: 현행 하네스는 사람이 읽는 7절 서술이고
  파서가 없다. 같은 이유로 아래 항목과 한 덩어리다
- **기계용 결과 계약(JSON 등) — 보류.** 근거: 현행 산출물 계약은 마크다운 + frontmatter 하나뿐이다.
  다만 그 전제(파이프에 장식이 나가지 않는다)는 이미 지켜지고 있다(19-7)
- **gemini-3.7·3.8 세대의 작은 글자 판독력 — 관찰 중.** 근거: 정렬 규칙상 자동 모드에서 이들이 맨 앞에 서지만,
  판독력 비교 실측은 저장소에 없다. 신형이 앞에 서는 근거는 판독력이 아니라 **생존율**이다(14장)
- **macOS 실기 — 미검증.** 근거: `test.yml`이 macos-latest를 돌지만 그것은 단위 테스트다.
  `run.sh` 더블클릭·`chmod` 경로의 실사용 검증은 없다고 워크플로 주석이 명시한다
